{"index.js":"function palindrome(str) {\n  // Good luck!\n  // let re = (/\\W/gi, '');\n\n  // console.log(\"Before: \" + str);\n  // Remove all of the non-alphanumeric characters \n  // Make the string all lower case\n  str = str.replace(/_|\\W/g, '').toLowerCase();\n  console.log(\"After: \" + str);\n\n  // Change the string to an array to be able to iterate through it\n  let newStr = str.split(\"\").reverse()\n  let newStr2 = newStr.join(\"\");\n  console.log(\"Last: \" + newStr2);\n\n  // let newerStr = [];\n  // console.log(\"Original: \" + newStr);\n  // Loop through the string adding to a new array\n  // for (var i = 0; i < newStr.length; i++){\n  //   newerStr.push(newStr[i]);\n  // }\n  if (str == newStr2) {\n    console.log(\"True\");\n    return true;\n  } else {\n    console.log(\"False\");\n    return false;\n  }\n}\n\n\n\n// palindrome(\"eye\");\npalindrome(\"not a palindrome\");\npalindrome(\"almostomla\");\npalindrome(\"nope\");\n// palindrome(\"0_0 (: /-\\ :) 0-0\");\n// palindrome(\"My age is 0, 0 si ega ym.\");"}